name: Current Affairs - Fact Extraction

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Test workflow
        run: |
          echo "========================================"
          echo "CURRENT AFFAIRS FACT EXTRACTION"
          echo "========================================"
          echo "Workflow is working!"
          echo "Branch: $GITHUB_REF_NAME"
          echo "Commit: $GITHUB_SHA"
          echo "========================================"
